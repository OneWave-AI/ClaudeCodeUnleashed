<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ClaudeCode Arena - The Ultimate GUI for Claude Code</title>
  <meta name="description" content="ClaudeCode Arena transforms the Claude Code CLI into a beautiful, powerful desktop experience. Track your coding time, manage multiple sessions, and let AI build while you focus.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    :root {
      /* Colors */
      --accent: #cc785c;
      --accent-light: #e8956e;
      --secondary: #22d3d1;
      --bg-dark: #030305;
      --bg-card: rgba(255, 255, 255, 0.03);
      --border: rgba(255, 255, 255, 0.08);
      --text: #e0e0e0;
      --text-muted: #888;

      /* Spacing Scale */
      --space-1: 4px;
      --space-2: 8px;
      --space-3: 12px;
      --space-4: 16px;
      --space-5: 24px;
      --space-6: 32px;
      --space-7: 48px;
      --space-8: 64px;
      --space-9: 96px;
      --space-10: 128px;

      /* Typography Scale */
      --text-xs: 0.75rem;
      --text-sm: 0.875rem;
      --text-base: 1rem;
      --text-lg: 1.125rem;
      --text-xl: 1.25rem;
      --text-2xl: 1.5rem;
      --text-3xl: 2rem;
      --text-4xl: 2.5rem;
      --text-5xl: 3rem;

      /* Letter Spacing */
      --tracking-heading: -0.02em;
      --tracking-body: 0;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: var(--bg-dark);
      color: var(--text);
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* Background effects */
    .bg-effects {
      position: fixed;
      inset: 0;
      pointer-events: none;
      z-index: -1;
    }

    .bg-effects::before {
      content: '';
      position: absolute;
      top: -20%;
      left: -10%;
      width: 500px;
      height: 500px;
      background: radial-gradient(ellipse, rgba(204, 120, 92, 0.15) 0%, transparent 70%);
      filter: blur(80px);
      animation: float 20s ease-in-out infinite;
      transform: translateY(var(--parallax-before, 0));
      transition: transform 0.1s linear;
    }

    .bg-effects::after {
      content: '';
      position: absolute;
      bottom: -10%;
      right: -10%;
      width: 400px;
      height: 400px;
      background: radial-gradient(ellipse, rgba(34, 211, 209, 0.1) 0%, transparent 70%);
      filter: blur(80px);
      animation: float 25s ease-in-out infinite reverse;
      transform: translateY(var(--parallax-after, 0));
      transition: transform 0.1s linear;
    }

    @keyframes float {
      0%, 100% { transform: translate(0, 0); }
      50% { transform: translate(30px, -30px); }
    }

    /* ========================================
       SCROLL REVEAL ANIMATIONS
       ======================================== */

    /* Base reveal class - elements start invisible and translated down */
    .reveal {
      opacity: 0;
      transform: translateY(40px);
      transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
      will-change: opacity, transform;
    }

    /* Active state - element animates into view */
    .reveal.active {
      opacity: 1;
      transform: translateY(0);
    }

    /* Reveal from left */
    .reveal-left {
      opacity: 0;
      transform: translateX(-60px);
      transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
      will-change: opacity, transform;
    }

    .reveal-left.active {
      opacity: 1;
      transform: translateX(0);
    }

    /* Reveal from right */
    .reveal-right {
      opacity: 0;
      transform: translateX(60px);
      transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
      will-change: opacity, transform;
    }

    .reveal-right.active {
      opacity: 1;
      transform: translateX(0);
    }

    /* Scale up reveal for download cards */
    .reveal-scale {
      opacity: 0;
      transform: scale(0.85);
      transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
      will-change: opacity, transform;
    }

    .reveal-scale.active {
      opacity: 1;
      transform: scale(1);
    }

    /* Fade in only (no movement) */
    .reveal-fade {
      opacity: 0;
      transition: opacity 0.6s ease-out;
      will-change: opacity;
    }

    .reveal-fade.active {
      opacity: 1;
    }

    /* Stagger delays for sequential animations */
    .stagger-1 { transition-delay: 0.05s; }
    .stagger-2 { transition-delay: 0.1s; }
    .stagger-3 { transition-delay: 0.15s; }
    .stagger-4 { transition-delay: 0.2s; }
    .stagger-5 { transition-delay: 0.25s; }
    .stagger-6 { transition-delay: 0.3s; }
    .stagger-7 { transition-delay: 0.35s; }
    .stagger-8 { transition-delay: 0.4s; }

    /* Hero badge shimmer animation */
    @keyframes shimmer {
      0% { background-position: -200% center; }
      100% { background-position: 200% center; }
    }

    @keyframes pulse-glow {
      0%, 100% { box-shadow: 0 4px 20px rgba(204, 120, 92, 0.3); }
      50% { box-shadow: 0 4px 30px rgba(204, 120, 92, 0.5), 0 0 40px rgba(204, 120, 92, 0.2); }
    }

    /* ========================================
       HERO ENTRANCE ANIMATIONS
       ======================================== */

    /* Badge floating animation */
    @keyframes badge-float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    /* Fade up animation */
    @keyframes hero-fade-up {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Simple fade in */
    @keyframes hero-fade-in {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    /* Slide up with fade */
    @keyframes hero-slide-up {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Gradient color shift animation */
    @keyframes gradient-shift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Text reveal with blur */
    @keyframes text-reveal {
      from {
        opacity: 0;
        transform: translateY(20px) rotateX(-10deg);
        filter: blur(4px);
      }
      to {
        opacity: 1;
        transform: translateY(0) rotateX(0);
        filter: blur(0);
      }
    }

    /* Particle floating animations */
    @keyframes particle-float-1 {
      0%, 100% {
        transform: translateY(0) translateX(0);
        opacity: 0;
      }
      10% { opacity: 0.6; }
      90% { opacity: 0.6; }
      100% {
        transform: translateY(-100vh) translateX(20px);
        opacity: 0;
      }
    }

    @keyframes particle-float-2 {
      0%, 100% {
        transform: translateY(0) translateX(0);
        opacity: 0;
      }
      10% { opacity: 0.4; }
      90% { opacity: 0.4; }
      100% {
        transform: translateY(-100vh) translateX(-15px);
        opacity: 0;
      }
    }

    @keyframes particle-float-3 {
      0%, 100% {
        transform: translateY(0) translateX(0);
        opacity: 0;
      }
      10% { opacity: 0.5; }
      90% { opacity: 0.5; }
      100% {
        transform: translateY(-100vh) translateX(10px);
        opacity: 0;
      }
    }

    /* Hero Particles Container */
    .hero-particles {
      position: absolute;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
      z-index: 0;
    }

    .hero-particle {
      position: absolute;
      border-radius: 50%;
      opacity: 0;
    }

    .hero-particle:nth-child(1) {
      left: 10%;
      bottom: 0;
      width: 4px;
      height: 4px;
      background: var(--accent);
      animation: particle-float-1 18s ease-in-out infinite;
      animation-delay: 0s;
    }

    .hero-particle:nth-child(2) {
      left: 25%;
      bottom: 0;
      width: 3px;
      height: 3px;
      background: var(--secondary);
      animation: particle-float-2 22s ease-in-out infinite;
      animation-delay: 2s;
    }

    .hero-particle:nth-child(3) {
      left: 40%;
      bottom: 0;
      width: 2px;
      height: 2px;
      background: rgba(255, 255, 255, 0.5);
      animation: particle-float-3 16s ease-in-out infinite;
      animation-delay: 4s;
    }

    .hero-particle:nth-child(4) {
      left: 55%;
      bottom: 0;
      width: 4px;
      height: 4px;
      background: var(--accent-light);
      animation: particle-float-1 20s ease-in-out infinite;
      animation-delay: 6s;
    }

    .hero-particle:nth-child(5) {
      left: 70%;
      bottom: 0;
      width: 3px;
      height: 3px;
      background: var(--secondary);
      animation: particle-float-2 24s ease-in-out infinite;
      animation-delay: 1s;
    }

    .hero-particle:nth-child(6) {
      left: 85%;
      bottom: 0;
      width: 2px;
      height: 2px;
      background: rgba(255, 255, 255, 0.4);
      animation: particle-float-3 19s ease-in-out infinite;
      animation-delay: 3s;
    }

    .hero-particle:nth-child(7) {
      left: 15%;
      bottom: 0;
      width: 3px;
      height: 3px;
      background: var(--accent);
      animation: particle-float-2 21s ease-in-out infinite;
      animation-delay: 5s;
    }

    .hero-particle:nth-child(8) {
      left: 60%;
      bottom: 0;
      width: 2px;
      height: 2px;
      background: rgba(255, 255, 255, 0.3);
      animation: particle-float-1 17s ease-in-out infinite;
      animation-delay: 7s;
    }

    .hero-particle:nth-child(9) {
      left: 35%;
      bottom: 0;
      width: 4px;
      height: 4px;
      background: var(--secondary);
      animation: particle-float-3 23s ease-in-out infinite;
      animation-delay: 8s;
    }

    .hero-particle:nth-child(10) {
      left: 80%;
      bottom: 0;
      width: 3px;
      height: 3px;
      background: var(--accent-light);
      animation: particle-float-1 25s ease-in-out infinite;
      animation-delay: 9s;
    }

    /* Global focus-visible styles for accessibility */
    :focus {
      outline: none;
    }

    :focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 3px;
      border-radius: 4px;
    }

    /* Container */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 var(--space-5);
    }

    /* Navigation */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      padding: var(--space-4) 0;
      background: rgba(3, 3, 5, 0.8);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
    }

    nav .container {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: var(--space-3);
      font-weight: 700;
      font-size: var(--text-xl);
      text-decoration: none;
      color: white;
      border-radius: 8px;
      padding: 4px 8px;
      margin: -4px -8px;
      transition: opacity 0.2s ease-out;
    }

    .logo:hover {
      opacity: 0.9;
    }

    .logo:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    .logo-icon {
      width: 36px;
      height: 36px;
      background: linear-gradient(135deg, var(--accent), var(--accent-light));
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .logo span:last-child {
      background: linear-gradient(90deg, var(--accent), var(--secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .nav-links {
      display: flex;
      gap: var(--space-6);
      align-items: center;
    }

    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: var(--text-sm);
      transition: color 0.25s ease-out;
      padding: 4px 8px;
      border-radius: 6px;
    }

    .nav-links a:hover {
      color: white;
    }

    .nav-links a:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
      padding: var(--space-3) var(--space-5);
      border-radius: 12px;
      font-weight: 600;
      font-size: var(--text-sm);
      text-decoration: none;
      transition: all 0.25s ease-out;
      cursor: pointer;
      border: none;
    }

    .btn:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 3px;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--accent), var(--accent-light));
      color: white;
      box-shadow: 0 4px 20px rgba(204, 120, 92, 0.3);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 30px rgba(204, 120, 92, 0.4);
    }

    .btn-primary:active {
      transform: translateY(0);
      box-shadow: 0 2px 10px rgba(204, 120, 92, 0.3);
    }

    /* Pulsing glow on primary download button */
    .btn-primary.pulse-glow {
      animation: pulse-glow 2s ease-in-out infinite;
    }

    .btn-secondary {
      background: var(--bg-card);
      color: white;
      border: 1px solid var(--border);
    }

    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.08);
      border-color: rgba(255, 255, 255, 0.15);
      transform: translateY(-1px);
    }

    .btn-secondary:active {
      transform: translateY(0);
      background: rgba(255, 255, 255, 0.05);
    }

    /* Hero Section */
    .hero {
      padding: calc(var(--space-10) + var(--space-8)) 0 var(--space-9);
      text-align: center;
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
      padding: var(--space-2) var(--space-4);
      background: linear-gradient(
        90deg,
        rgba(34, 211, 209, 0.1) 0%,
        rgba(34, 211, 209, 0.2) 25%,
        rgba(204, 120, 92, 0.15) 50%,
        rgba(34, 211, 209, 0.2) 75%,
        rgba(34, 211, 209, 0.1) 100%
      );
      background-size: 200% auto;
      border: 1px solid rgba(34, 211, 209, 0.2);
      border-radius: 100px;
      font-size: var(--text-sm);
      color: var(--secondary);
      margin-bottom: var(--space-5);
      /* Entrance: 0s delay - appears first */
      opacity: 0;
      animation:
        hero-fade-up 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards,
        badge-float 4s ease-in-out 1s infinite,
        shimmer 3s linear infinite;
      animation-delay: 0s, 1s, 0s;
    }

    .hero h1 {
      font-size: clamp(2.5rem, 6vw, 4rem);
      font-weight: 800;
      line-height: 1.1;
      margin-bottom: var(--space-5);
      letter-spacing: -0.02em;
      perspective: 1000px;
    }

    /* Text lines for staggered reveal */
    .hero h1 .text-line {
      display: block;
      opacity: 0;
      animation: text-reveal 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }

    .hero h1 .text-line:first-child {
      /* Entrance: 0.2s delay */
      animation-delay: 0.2s;
    }

    .hero h1 .text-line:last-child {
      /* Entrance: 0.35s delay */
      animation-delay: 0.35s;
    }

    .hero h1 .gradient {
      background: linear-gradient(135deg, var(--accent), var(--accent-light), var(--secondary), var(--accent));
      background-size: 300% 300%;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      animation: gradient-shift 8s ease infinite;
    }

    /* Hero subtext animation */
    .hero-subtext {
      opacity: 0;
      /* Entrance: 0.4s delay */
      animation: hero-slide-up 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.4s forwards;
    }

    .hero p {
      font-size: var(--text-xl);
      color: var(--text-muted);
      max-width: 65ch;
      margin: 0 auto var(--space-7);
    }

    .hero-buttons {
      display: flex;
      gap: var(--space-4);
      justify-content: center;
      flex-wrap: wrap;
    }

    /* Button entrance animations */
    .hero-buttons .btn {
      opacity: 0;
    }

    .hero-buttons .btn:first-child {
      /* Entrance: 0.6s delay */
      animation: hero-slide-up 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.6s forwards;
    }

    .hero-buttons .btn:nth-child(2) {
      /* Entrance: 0.7s delay - staggered */
      animation: hero-slide-up 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.7s forwards;
    }

    /* MIT badge entrance animation */
    .hero-mit-badge {
      opacity: 0;
      /* Entrance: 0.8s delay - appears last */
      animation: hero-fade-in 0.5s ease 0.8s forwards;
    }

    .hero-image {
      margin-top: var(--space-8);
      border-radius: 16px;
      overflow: hidden;
      border: 1px solid var(--border);
      box-shadow: 0 40px 80px rgba(0, 0, 0, 0.5);
    }

    .hero-image img {
      width: 100%;
      height: auto;
      display: block;
    }

    /* Stats Bar */
    .stats-bar {
      background: rgba(255, 255, 255, 0.02);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      padding: var(--space-6) 0;
    }

    .stats-grid {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: var(--space-8);
      flex-wrap: wrap;
    }

    .stat-item {
      display: flex;
      align-items: center;
      gap: var(--space-3);
      color: var(--text-muted);
      font-size: var(--text-sm);
    }

    .stat-item svg {
      color: var(--accent);
      flex-shrink: 0;
    }

    .stat-item strong {
      color: var(--text);
      font-weight: 600;
    }

    /* Features Section */
    .section {
      padding: var(--space-9) 0;
    }

    .section-header {
      text-align: center;
      margin-bottom: var(--space-8);
    }

    .section-header h2 {
      font-size: var(--text-4xl);
      font-weight: 700;
      letter-spacing: var(--tracking-heading);
      margin-bottom: var(--space-4);
    }

    .section-header p {
      color: var(--text-muted);
      font-size: var(--text-lg);
      max-width: 65ch;
      margin: 0 auto;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: var(--space-5);
    }

    .feature-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: var(--space-6);
      transition: all 0.25s ease-out;
    }

    .feature-card:hover {
      border-color: rgba(204, 120, 92, 0.3);
      transform: translateY(-4px);
      box-shadow: 0 8px 30px rgba(204, 120, 92, 0.1), 0 0 0 1px rgba(204, 120, 92, 0.1);
    }

    .feature-card:focus-within {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    .feature-icon {
      width: var(--space-7);
      height: var(--space-7);
      border-radius: var(--space-3);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: var(--space-5);
      font-size: var(--text-2xl);
    }

    .feature-icon.orange { background: rgba(204, 120, 92, 0.15); }
    .feature-icon.teal { background: rgba(34, 211, 209, 0.15); }
    .feature-icon.blue { background: rgba(96, 165, 250, 0.15); }
    .feature-icon.green { background: rgba(52, 211, 153, 0.15); }

    .feature-card h3 {
      font-size: var(--text-xl);
      font-weight: 600;
      letter-spacing: var(--tracking-heading);
      margin-bottom: var(--space-3);
    }

    .feature-card p {
      color: var(--text-muted);
      max-width: 65ch;
      letter-spacing: var(--tracking-body);
      font-size: var(--text-sm);
    }

    /* Comparison Section */
    .comparison {
      background: var(--bg-card);
      border-radius: var(--space-5);
      border: 1px solid var(--border);
      padding: var(--space-8);
      margin-top: var(--space-7);
    }

    .comparison-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-8);
    }

    .comparison-col h3 {
      font-size: var(--text-2xl);
      margin-bottom: var(--space-5);
      letter-spacing: var(--tracking-heading);
      display: flex;
      align-items: center;
      gap: var(--space-3);
    }

    .comparison-col.arena h3 {
      color: var(--accent);
    }

    .comparison-list {
      list-style: none;
    }

    .comparison-list li {
      display: flex;
      align-items: flex-start;
      gap: var(--space-3);
      padding: var(--space-3) 0;
      border-bottom: 1px solid var(--border);
    }

    .comparison-list li:last-child {
      border-bottom: none;
    }

    .check {
      color: #34d399;
      font-size: 1.2rem;
    }

    .x {
      color: #f87171;
      font-size: 1.2rem;
    }

    /* Analytics Section */
    .analytics-preview {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-7);
      align-items: center;
      margin-top: var(--space-8);
    }

    .analytics-content h3 {
      font-size: var(--text-3xl);
      letter-spacing: var(--tracking-heading);
      margin-bottom: var(--space-4);
    }

    .analytics-content p {
      color: var(--text-muted);
      max-width: 65ch;
      margin-bottom: var(--space-5);
    }

    .analytics-features {
      list-style: none;
    }

    .analytics-features li {
      display: flex;
      align-items: center;
      gap: var(--space-3);
      padding: var(--space-2) 0;
      color: var(--text-muted);
    }

    .analytics-features li span {
      color: var(--accent);
    }

    /* Download Section */
    .download {
      text-align: center;
      padding: var(--space-9) 0;
    }

    .download h2 {
      font-size: var(--text-4xl);
      letter-spacing: var(--tracking-heading);
      margin-bottom: var(--space-4);
    }

    .download p {
      color: var(--text-muted);
      margin-bottom: var(--space-7);
      font-size: var(--text-lg);
    }

    .download-cards {
      display: flex;
      gap: var(--space-5);
      justify-content: center;
      flex-wrap: wrap;
    }

    .download-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: var(--space-4);
      padding: var(--space-6) var(--space-7);
      text-align: center;
      min-width: 280px;
      transition: all 0.25s ease-out;
    }

    .download-card:hover {
      border-color: var(--accent);
      transform: translateY(-4px) scale(1.02);
      box-shadow: 0 12px 40px rgba(204, 120, 92, 0.15);
    }

    .download-card:focus-within {
      outline: 2px solid var(--accent);
      outline-offset: 3px;
    }

    .download-card .icon {
      font-size: var(--text-5xl);
      margin-bottom: var(--space-4);
    }

    .download-card h4 {
      font-size: var(--text-xl);
      letter-spacing: var(--tracking-heading);
      margin-bottom: var(--space-2);
    }

    .download-card p {
      color: var(--text-muted);
      font-size: var(--text-sm);
      margin-bottom: var(--space-5);
    }

    /* Requirements */
    .requirements {
      background: var(--bg-card);
      border-radius: var(--space-4);
      border: 1px solid var(--border);
      padding: var(--space-7);
      margin-top: var(--space-8);
      text-align: left;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .requirements h4 {
      margin-bottom: var(--space-4);
    }

    .requirements ul {
      list-style: none;
      color: var(--text-muted);
    }

    .requirements li {
      padding: var(--space-2) 0;
      display: flex;
      align-items: center;
      gap: var(--space-3);
    }

    /* Developer Tagline */
    .developer-tagline {
      text-align: center;
      padding: var(--space-8) 0;
      border-top: 1px solid var(--border);
    }

    .developer-tagline h3 {
      font-size: var(--text-2xl);
      font-weight: 700;
      letter-spacing: var(--tracking-heading);
      margin-bottom: var(--space-3);
      background: linear-gradient(135deg, var(--accent), var(--secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .developer-tagline p {
      color: var(--text-muted);
      font-size: var(--text-base);
    }

    /* Footer */
    footer {
      padding: var(--space-7) 0;
      border-top: 1px solid var(--border);
      text-align: center;
      color: var(--text-muted);
      font-size: var(--text-sm);
    }

    .footer-links {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: var(--space-5);
      flex-wrap: wrap;
      margin-bottom: var(--space-4);
    }

    .footer-links a {
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
      color: var(--text-muted);
      text-decoration: none;
      transition: color 0.2s ease-out;
    }

    .footer-links a:hover {
      color: var(--accent);
    }

    .footer-links a svg {
      width: 16px;
      height: 16px;
    }

    .claude-badge {
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
      padding: var(--space-2) var(--space-4);
      background: rgba(34, 211, 209, 0.1);
      border: 1px solid rgba(34, 211, 209, 0.2);
      border-radius: 100px;
      font-size: var(--text-xs);
      color: var(--secondary);
      margin-top: var(--space-4);
    }

    footer a {
      color: var(--accent);
      text-decoration: none;
      transition: color 0.2s ease-out, text-decoration 0.2s ease-out;
    }

    footer a:hover {
      color: var(--accent-light);
      text-decoration: underline;
    }

    footer a:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
      border-radius: 4px;
    }

    /* Mobile Menu Button */
    .mobile-menu-btn {
      display: none;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      width: 44px;
      height: 44px;
      background: transparent;
      border: 1px solid var(--border);
      border-radius: 8px;
      cursor: pointer;
      gap: 5px;
      padding: 0;
    }

    .mobile-menu-btn:hover {
      background: rgba(255, 255, 255, 0.05);
    }

    .mobile-menu-btn:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    .mobile-menu-btn span {
      display: block;
      width: 20px;
      height: 2px;
      background: var(--text);
      border-radius: 2px;
      transition: all 0.3s ease;
    }

    .mobile-menu-btn.active span:nth-child(1) {
      transform: rotate(45deg) translate(5px, 5px);
    }

    .mobile-menu-btn.active span:nth-child(2) {
      opacity: 0;
    }

    .mobile-menu-btn.active span:nth-child(3) {
      transform: rotate(-45deg) translate(5px, -5px);
    }

    /* Mobile Navigation Overlay */
    .mobile-nav {
      display: none;
      position: fixed;
      top: 73px;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(3, 3, 5, 0.98);
      backdrop-filter: blur(20px);
      z-index: 99;
      flex-direction: column;
      padding: 24px;
      opacity: 0;
      visibility: hidden;
      transition: opacity 0.3s ease, visibility 0.3s ease;
    }

    .mobile-nav.active {
      opacity: 1;
      visibility: visible;
    }

    .mobile-nav a {
      display: block;
      padding: 16px 0;
      color: var(--text);
      text-decoration: none;
      font-size: 1.1rem;
      font-weight: 500;
      border-bottom: 1px solid var(--border);
      transition: color 0.2s;
    }

    .mobile-nav a:hover {
      color: var(--accent);
    }

    .mobile-nav a:last-child {
      border-bottom: none;
      margin-top: 16px;
    }

    .mobile-nav .btn {
      text-align: center;
      justify-content: center;
    }

    /* Tablet Breakpoint */
    @media (max-width: 1024px) {
      .features-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .comparison-grid {
        gap: var(--space-7);
      }

      .comparison {
        padding: var(--space-7) var(--space-6);
      }

      .analytics-preview {
        gap: var(--space-6);
      }

      .section {
        padding: 80px 0;
      }

      .hero {
        padding: 140px 0 80px;
      }

      .download {
        padding: 80px 0;
      }

      .section-header h2 {
        font-size: 2rem;
      }
    }

    /* Mobile Breakpoint */
    @media (max-width: 768px) {
      .nav-links {
        display: none;
      }

      .mobile-menu-btn {
        display: flex;
      }

      .mobile-nav {
        display: flex;
      }

      .section {
        padding: 60px 0;
      }

      .hero {
        padding: 120px 0 60px;
      }

      .hero h1 {
        font-size: 2rem;
      }

      .hero p {
        font-size: 1rem;
      }

      .hero-buttons {
        flex-direction: column;
        align-items: center;
      }

      .hero-buttons .btn {
        width: 100%;
        max-width: 280px;
        justify-content: center;
      }

      .section-header {
        margin-bottom: 40px;
      }

      .section-header h2 {
        font-size: 1.75rem;
      }

      .section-header p {
        font-size: 1rem;
      }

      .features-grid {
        grid-template-columns: 1fr;
        gap: 16px;
      }

      .feature-card {
        padding: 24px;
      }

      .comparison-grid {
        grid-template-columns: 1fr;
        gap: var(--space-6);
      }

      .comparison {
        padding: var(--space-6) var(--space-5);
      }

      .comparison-col h3 {
        font-size: 1.25rem;
      }

      .analytics-preview {
        grid-template-columns: 1fr;
        gap: var(--space-5);
      }

      .analytics-content h3 {
        font-size: 1.5rem;
      }

      .download {
        padding: 60px 0;
      }

      .download h2 {
        font-size: 1.75rem;
      }

      .download-cards {
        flex-direction: column;
        align-items: center;
      }

      .download-card {
        width: 100%;
        max-width: 320px;
        padding: 24px;
      }

      .requirements {
        padding: 24px;
        margin-top: 40px;
      }

      .container {
        padding: 0 16px;
      }

      footer {
        padding: 32px 0;
      }
    }

    /* Small Mobile Breakpoint */
    @media (max-width: 480px) {
      .hero h1 {
        font-size: 1.75rem;
      }

      .hero-badge {
        font-size: 0.75rem;
        padding: 6px 12px;
      }

      .feature-card {
        padding: 20px;
      }

      .feature-icon {
        width: 40px;
        height: 40px;
        font-size: 1.25rem;
      }

      .feature-card h3 {
        font-size: 1.1rem;
      }

      .download-card {
        padding: 20px;
      }

      .requirements code {
        font-size: 0.7rem;
      }
    }
  </style>
</head>
<body>
  <div class="bg-effects"></div>

  <!-- Mobile Navigation Overlay -->
  <div class="mobile-nav" id="mobileNav">
    <a href="#features" onclick="closeMobileMenu()">Features</a>
    <a href="#comparison" onclick="closeMobileMenu()">Why Arena?</a>
    <a href="#analytics" onclick="closeMobileMenu()">Analytics</a>
    <a href="#open-source" onclick="closeMobileMenu()">Open Source</a>
    <a href="#download" class="btn btn-primary" onclick="closeMobileMenu()">Get it Free</a>
  </div>

  <!-- Navigation -->
  <nav>
    <div class="container">
      <a href="#" class="logo">
        <div class="logo-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="16 18 22 12 16 6"></polyline>
            <polyline points="8 6 2 12 8 18"></polyline>
          </svg>
        </div>
        <span>ClaudeCode</span>
        <span>Arena</span>
      </a>
      <div class="nav-links">
        <a href="#features">Features</a>
        <a href="#comparison">Why Arena?</a>
        <a href="#analytics">Analytics</a>
        <a href="#open-source">Open Source</a>
        <a href="#download" class="btn btn-primary">Get it Free</a>
      </div>
      <button class="mobile-menu-btn" id="mobileMenuBtn" aria-label="Toggle mobile menu" onclick="toggleMobileMenu()">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero" style="position: relative;">
    <!-- Floating Particles -->
    <div class="hero-particles">
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
      <div class="hero-particle"></div>
    </div>
    <div class="container" style="position: relative; z-index: 1;">
      <div class="hero-badge">
        <span>100% Free &amp; Open Source</span>
      </div>
      <h1>
        <span class="text-line">The Ultimate GUI for</span>
        <span class="text-line"><span class="gradient">Claude Code</span></span>
      </h1>
      <p class="hero-subtext">
        No subscriptions. No payments. Just a beautiful, powerful desktop app for Claude Code.
        Get built-in analytics, multi-agent swarms, split terminals, and more - all free under MIT license. Fork it, extend it, make it yours.
      </p>
      <div class="hero-buttons">
        <a href="#download" class="btn btn-primary pulse-glow">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="7 10 12 15 17 10"></polyline>
            <line x1="12" y1="15" x2="12" y2="3"></line>
          </svg>
          Download for Mac
        </a>
        <a href="https://github.com" class="btn btn-secondary">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          Star on GitHub
        </a>
      </div>
      <div class="hero-mit-badge" style="margin-top: 16px;">
        <span style="display: inline-flex; align-items: center; gap: 6px; padding: 4px 12px; background: rgba(52, 211, 153, 0.1); border: 1px solid rgba(52, 211, 153, 0.2); border-radius: 100px; font-size: 0.75rem; color: #34d399;">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
          </svg>
          MIT License
        </span>
      </div>
    </div>
  </section>

  <!-- Stats Bar -->
  <div class="stats-bar">
    <div class="container">
      <div class="stats-grid">
        <div class="stat-item reveal-fade stagger-1">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="7 10 12 15 17 10"></polyline>
            <line x1="12" y1="15" x2="12" y2="3"></line>
          </svg>
          <span><strong>500+</strong> Downloads</span>
        </div>
        <div class="stat-item reveal-fade stagger-2">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
            <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
          </svg>
          <span><strong>MIT</strong> Licensed</span>
        </div>
        <div class="stat-item reveal-fade stagger-3">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"></circle>
            <path d="M8 14s1.5 2 4 2 4-2 4-2"></path>
            <line x1="9" y1="9" x2="9.01" y2="9"></line>
            <line x1="15" y1="9" x2="15.01" y2="9"></line>
          </svg>
          <span><strong>Built with</strong> Claude</span>
        </div>
        <div class="stat-item reveal-fade stagger-4">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
            <line x1="8" y1="21" x2="16" y2="21"></line>
            <line x1="12" y1="17" x2="12" y2="21"></line>
          </svg>
          <span><strong>macOS</strong>, Windows, Linux</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Features Section -->
  <section id="features" class="section">
    <div class="container">
      <div class="section-header reveal">
        <h2>Everything the CLI is missing</h2>
        <p>All the features you wish Claude Code had - built in, polished, and completely FREE.</p>
      </div>

      <div class="features-grid">
        <div class="feature-card reveal stagger-1">
          <div class="feature-icon orange">🖥️</div>
          <h3>Stop Squinting at Plain Terminals <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>Your eyes deserve better. Custom themes, syntax highlighting, and smooth animations turn coding sessions into a pleasure, not a chore.</p>
        </div>

        <div class="feature-card reveal stagger-2">
          <div class="feature-icon teal">📊</div>
          <h3>Bill Clients Accurately <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>Never guess your hours again. Track every minute spent coding per project automatically. Export detailed reports for invoicing in seconds.</p>
        </div>

        <div class="feature-card reveal stagger-3">
          <div class="feature-icon blue">🔀</div>
          <h3>Work on Two Things at Once <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>Frontend and backend. Bug fix and new feature. Split panels let you run parallel AI sessions without tab chaos or lost context.</p>
        </div>

        <div class="feature-card reveal stagger-4">
          <div class="feature-icon green">🤖</div>
          <h3>Hands-Free Coding <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>Let AI work autonomously while you grab coffee. Super Agent auto-approves safe actions with configurable time limits and safety rails.</p>
        </div>

        <div class="feature-card reveal stagger-5">
          <div class="feature-icon orange">📁</div>
          <h3>Stop Typing File Paths <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>See your project. Click your files. Drag and drop into chat. Visual file browsing beats typing "../../../src/components" every time.</p>
        </div>

        <div class="feature-card reveal stagger-6">
          <div class="feature-icon teal">🔧</div>
          <h3>Customize Without Config Files <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>Create and manage skills visually. No more editing JSON in hidden folders. Import community agents with one click.</p>
        </div>

        <div class="feature-card reveal stagger-7">
          <div class="feature-icon blue">🌐</div>
          <h3>See Changes Instantly <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>Built-in browser auto-detects localhost URLs. Watch your app update live as Claude writes code - no manual refresh needed.</p>
        </div>

        <div class="feature-card reveal stagger-8">
          <div class="feature-icon green">⚡</div>
          <h3>Switch Models in One Click <span style="background: rgba(52, 211, 153, 0.2); color: #34d399; font-size: 0.7rem; padding: 2px 8px; border-radius: 100px; margin-left: 8px; font-weight: 500;">FREE</span></h3>
          <p>Opus for complex architecture. Sonnet for daily tasks. Haiku for quick fixes. Toggle instantly without typing commands.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Comparison Section -->
  <section id="comparison" class="section">
    <div class="container">
      <div class="section-header">
        <h2>CLI Power + Visual Comfort</h2>
        <p>Arena wraps the official Claude Code CLI, giving you all its power with a beautiful visual interface.</p>
      </div>

      <div class="comparison">
        <div class="comparison-grid">
          <div class="comparison-col arena">
            <h3>
              <span class="logo-icon" style="width:32px;height:32px;">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5">
                  <polyline points="16 18 22 12 16 6"></polyline>
                  <polyline points="8 6 2 12 8 18"></polyline>
                </svg>
              </span>
              ClaudeCode Arena
            </h3>
            <ul class="comparison-list">
              <li><span class="check">✓</span> Beautiful native Mac app with custom themes</li>
              <li><span class="check">✓</span> Track coding time per project for billing</li>
              <li><span class="check">✓</span> Split panels for multiple sessions</li>
              <li><span class="check">✓</span> Visual file browser with drag & drop</li>
              <li><span class="check">✓</span> Super Agent for autonomous execution</li>
              <li><span class="check">✓</span> Built-in localhost preview</li>
              <li><span class="check">✓</span> Session history with search</li>
              <li><span class="check">✓</span> One-click model switching</li>
              <li><span class="check">✓</span> MCP server configuration UI</li>
              <li><span class="check">✓</span> Skills & Agents visual editor</li>
            </ul>
          </div>

          <div class="comparison-col">
            <h3>
              <span style="font-size:1.5rem;">⬛</span>
              Standard Claude Code CLI
            </h3>
            <ul class="comparison-list">
              <li><span class="x">✗</span> Plain terminal interface</li>
              <li><span class="x">✗</span> No time tracking or analytics</li>
              <li><span class="x">✗</span> One session per terminal window</li>
              <li><span class="x">✗</span> Manual file path typing</li>
              <li><span class="x">✗</span> Manual approval for each action</li>
              <li><span class="x">✗</span> Open preview URLs manually</li>
              <li><span class="x">✗</span> History via terminal scrollback</li>
              <li><span class="x">✗</span> Type /model command each time</li>
              <li><span class="x">✗</span> Edit JSON config files manually</li>
              <li><span class="x">✗</span> Create markdown files manually</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Analytics Section -->
  <section id="analytics" class="section">
    <div class="container">
      <div class="section-header">
        <h2>Track Every Minute</h2>
        <p>Know exactly how much time you spend on each project. Perfect for freelancers, consultants, and teams.</p>
      </div>

      <div class="analytics-preview">
        <div class="analytics-content">
          <h3>Developer Time Tracking Built-In</h3>
          <p>
            ClaudeCode Arena automatically tracks your coding sessions with real timestamps.
            See which projects take the most time, identify your most productive days, and generate reports for clients.
          </p>
          <ul class="analytics-features">
            <li><span>📈</span> 7-day and 30-day activity charts</li>
            <li><span>⏱️</span> Accurate time tracking per project</li>
            <li><span>📊</span> Session count and token usage</li>
            <li><span>🗂️</span> Projects sorted by time spent</li>
            <li><span>📅</span> Peak productivity hours analysis</li>
            <li><span>🔍</span> Detailed session history browser</li>
          </ul>
        </div>
        <div style="background: var(--bg-card); border: 1px solid var(--border); border-radius: 16px; padding: 24px;">
          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 20px;">
            <div style="background: rgba(204,120,92,0.1); padding: 16px; border-radius: 12px;">
              <div style="font-size: 0.75rem; color: var(--text-muted); margin-bottom: 4px;">This Week</div>
              <div style="font-size: 1.75rem; font-weight: 700; color: var(--accent);">47</div>
              <div style="font-size: 0.8rem; color: var(--text-muted);">sessions</div>
            </div>
            <div style="background: rgba(34,211,209,0.1); padding: 16px; border-radius: 12px;">
              <div style="font-size: 0.75rem; color: var(--text-muted); margin-bottom: 4px;">Time Coded</div>
              <div style="font-size: 1.75rem; font-weight: 700; color: var(--secondary);">12h 30m</div>
              <div style="font-size: 0.8rem; color: var(--text-muted);">this week</div>
            </div>
          </div>
          <div style="font-size: 0.75rem; color: var(--text-muted); margin-bottom: 12px; text-transform: uppercase; letter-spacing: 0.05em;">Top Projects</div>
          <div style="space-y: 8px;">
            <div style="display: flex; align-items: center; gap: 12px; padding: 8px 0; border-bottom: 1px solid var(--border);">
              <span style="color: var(--text-muted);">1.</span>
              <span style="flex: 1;">claude-ui</span>
              <span style="color: var(--accent); font-weight: 600;">4h 20m</span>
            </div>
            <div style="display: flex; align-items: center; gap: 12px; padding: 8px 0; border-bottom: 1px solid var(--border);">
              <span style="color: var(--text-muted);">2.</span>
              <span style="flex: 1;">api-server</span>
              <span style="color: var(--accent); font-weight: 600;">2h 15m</span>
            </div>
            <div style="display: flex; align-items: center; gap: 12px; padding: 8px 0;">
              <span style="color: var(--text-muted);">3.</span>
              <span style="flex: 1;">docs</span>
              <span style="color: var(--accent); font-weight: 600;">1h 45m</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Open Source Section -->
  <section id="open-source" class="section">
    <div class="container">
      <div class="section-header">
        <h2>100% Open Source</h2>
        <p>ClaudeCode Arena is released under the MIT license. Free to use, modify, and distribute.</p>
      </div>

      <div class="features-grid" style="grid-template-columns: repeat(3, 1fr);">
        <div class="feature-card" style="text-align: center;">
          <div class="feature-icon green" style="margin-left: auto; margin-right: auto; font-size: 2rem; width: 64px; height: 64px;">
            <span style="line-height: 1;">&#128176;</span>
          </div>
          <h3>Free Forever</h3>
          <p>No subscriptions, no hidden fees, no premium tiers. The full experience is completely free for everyone.</p>
        </div>

        <div class="feature-card" style="text-align: center;">
          <div class="feature-icon teal" style="margin-left: auto; margin-right: auto; font-size: 2rem; width: 64px; height: 64px;">
            <span style="line-height: 1;">&#128220;</span>
          </div>
          <h3>MIT Licensed</h3>
          <p>Fork it, modify it, build your own version. Do whatever you want with the code, no strings attached.</p>
        </div>

        <div class="feature-card" style="text-align: center;">
          <div class="feature-icon orange" style="margin-left: auto; margin-right: auto; font-size: 2rem; width: 64px; height: 64px;">
            <span style="line-height: 1;">&#129309;</span>
          </div>
          <h3>Community Driven</h3>
          <p>Contributions welcome, issues tracked on GitHub. Help shape the future of AI-assisted development.</p>
        </div>
      </div>

      <div style="text-align: center; margin-top: var(--space-7);">
        <a href="https://github.com" class="btn btn-secondary" style="padding: var(--space-4) var(--space-6);">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          View Source on GitHub
        </a>
      </div>
    </div>
  </section>

  <!-- Download Section -->
  <section id="download" class="download">
    <div class="container">
      <h2>Download Free</h2>
      <p>No account required - No subscription - Just download and code</p>

      <div class="download-cards">
        <div class="download-card" style="border-color: var(--accent); box-shadow: 0 0 30px rgba(204, 120, 92, 0.2);">
          <div class="icon">🍎</div>
          <h4>macOS (Apple Silicon)</h4>
          <p>For M1, M2, M3 Macs</p>
          <a href="ClaudeCodeArena-2.0.0-arm64.dmg" class="btn btn-primary pulse-glow" style="padding: 16px 32px; font-size: 1rem;" download>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
              <polyline points="7 10 12 15 17 10"></polyline>
              <line x1="12" y1="15" x2="12" y2="3"></line>
            </svg>
            Download Free for Mac
          </a>
        </div>

        <div class="download-card">
          <div class="icon">💻</div>
          <h4>macOS (Intel)</h4>
          <p>For Intel-based Macs</p>
          <a href="#" class="btn btn-secondary">Coming Soon</a>
        </div>

        <div class="download-card">
          <div class="icon">🪟</div>
          <h4>Windows</h4>
          <p>Windows 10/11</p>
          <a href="#" class="btn btn-secondary">Coming Soon</a>
        </div>
      </div>

      <div class="requirements">
        <h4>Requirements</h4>
        <ul>
          <li>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2">
              <polyline points="20 6 9 17 4 12"></polyline>
            </svg>
            macOS 12.0 or later
          </li>
          <li>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2">
              <polyline points="20 6 9 17 4 12"></polyline>
            </svg>
            Claude Code CLI installed (<code style="background: var(--bg-card); padding: 2px 6px; border-radius: 4px;">npm i -g @anthropic-ai/claude-code</code>)
          </li>
          <li>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2">
              <polyline points="20 6 9 17 4 12"></polyline>
            </svg>
            Anthropic API key configured
          </li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Developer Tagline -->
  <section class="developer-tagline">
    <div class="container">
      <h3>Built by developers, for developers</h3>
      <p>Join the community of developers shipping faster with AI-assisted coding.</p>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div class="container">
      <div class="footer-links">
        <a href="https://github.com/anthropics/claude-code-ui/issues" target="_blank" rel="noopener noreferrer">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="12" y1="8" x2="12" y2="12"></line>
            <line x1="12" y1="16" x2="12.01" y2="16"></line>
          </svg>
          Report Issues
        </a>
        <a href="https://github.com/anthropics/claude-code-ui/pulls" target="_blank" rel="noopener noreferrer">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="18" cy="18" r="3"></circle>
            <circle cx="6" cy="6" r="3"></circle>
            <path d="M6 21V9a9 9 0 0 0 9 9"></path>
          </svg>
          Contribute
        </a>
        <a href="https://github.com/anthropics/claude-code-ui" target="_blank" rel="noopener noreferrer">
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          Star on GitHub
        </a>
      </div>
      <p>
        ClaudeCode Arena is an open-source project. Built with Electron, React, and Claude.
      </p>
      <div class="claude-badge">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 2L2 7l10 5 10-5-10-5z"></path>
          <path d="M2 17l10 5 10-5"></path>
          <path d="M2 12l10 5 10-5"></path>
        </svg>
        Made with Claude Code
      </div>
    </div>
  </footer>

  <!-- Mobile Menu JavaScript & Scroll Animations -->
  <script>
    // ========================================
    // SCROLL REVEAL ANIMATIONS
    // ========================================

    // Intersection Observer for reveal animations
    const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('active');
        }
      });
    }, {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    });

    // Observe all reveal elements
    document.addEventListener('DOMContentLoaded', function() {
      const revealElements = document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .reveal-scale, .reveal-fade');
      revealElements.forEach(el => revealObserver.observe(el));
    });

    // Parallax effect for background blobs
    let ticking = false;
    window.addEventListener('scroll', function() {
      if (!ticking) {
        window.requestAnimationFrame(function() {
          const scrollY = window.scrollY;
          const bgEffects = document.querySelector('.bg-effects');
          if (bgEffects) {
            // Move blobs at different rates for parallax effect
            const beforeY = scrollY * 0.3;
            const afterY = scrollY * 0.2;
            bgEffects.style.setProperty('--parallax-before', `${beforeY}px`);
            bgEffects.style.setProperty('--parallax-after', `${afterY}px`);
          }
          ticking = false;
        });
        ticking = true;
      }
    });

    // Mobile menu functions
    function toggleMobileMenu() {
      const btn = document.getElementById('mobileMenuBtn');
      const nav = document.getElementById('mobileNav');
      btn.classList.toggle('active');
      nav.classList.toggle('active');

      // Prevent body scroll when menu is open
      if (nav.classList.contains('active')) {
        document.body.style.overflow = 'hidden';
      } else {
        document.body.style.overflow = '';
      }
    }

    function closeMobileMenu() {
      const btn = document.getElementById('mobileMenuBtn');
      const nav = document.getElementById('mobileNav');
      btn.classList.remove('active');
      nav.classList.remove('active');
      document.body.style.overflow = '';
    }

    // Close menu when clicking outside
    document.addEventListener('click', function(e) {
      const nav = document.getElementById('mobileNav');
      const btn = document.getElementById('mobileMenuBtn');
      if (nav.classList.contains('active') &&
          !nav.contains(e.target) &&
          !btn.contains(e.target)) {
        closeMobileMenu();
      }
    });

    // Close menu on escape key
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape') {
        closeMobileMenu();
      }
    });

    // Close menu on window resize above mobile breakpoint
    window.addEventListener('resize', function() {
      if (window.innerWidth > 768) {
        closeMobileMenu();
      }
    });
  </script>
</body>
</html>
